import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Mail = () => {
  return (
    <SafeAreaView>
      <Text>Mails</Text>
    </SafeAreaView>
  );
};

export default Mail;

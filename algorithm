
Algorithm : Process Mail Transfer and Distribution
Input: Transaction details including customer details, mail details and recepient details
Output: The mail item successfully delivered to the recepient
Begin: 
    Enter customer details  
    costs = calculatecost(mailType, weight)
    Add multiple mail items
    Generate receipt
    Store transaction details.
    bundle = createBundle(destPostOffice)
    barcode = generateBarcode(mailID)
    Dispatchbundles(bundleID)
    updateBundleStatus(bundlID)
    notifyPostman(mailID)
    Deliver(){
        if(success){
            update status
        }else(attempts > 2){
            contactcustomer()
            return or change address
        }
    }
    checkStatusofMailItem(mailID)
End;



